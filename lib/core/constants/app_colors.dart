import 'package:flutter/material.dart';

class AppColors {
  /// Primary brand color
  static const Color primary = Color(0xFF0066FF);

  /// Accent / Highlight color
  static const Color accent = Color(0xFF00C853);

  /// Background color used for main screens
  static const Color background = Color(0xFFF5F7FA);

  /// Dark text for high contrast
  static const Color textDark = Color(0xFF1A1A1A);

  /// Medium contrast text
  static const Color textMedium = Color(0xFF4A4A4A);

  /// Light text (secondary info)
  static const Color textLight = Color(0xFF9E9E9E);

  /// Card and surface UI background
  static const Color card = Color(0xFFFFFFFF);

  /// Error indication color
  static const Color error = Color(0xFFD32F2F);

  /// Success indication color
  static const Color success = Color(0xFF2E7D32);

  /// Light shadow color for elevation effects
  static const Color shadow = Color(0x1A000000);
}
